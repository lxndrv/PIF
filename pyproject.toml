[project]
name = "pif-experiments"
version = "0.1.0"
description = "AI hallucination-threshold case-study experiments"
requires-python = "==3.12.*"

# RIGHT-SIZED, NOT REMOVED. This venv was 426 MiB serving exactly two scripts under
# case-studies/ai-hallucination-threshold/. The plan's sweep reported PIF as having no
# Python at all; re-verification found the two scripts, which changes the correct action
# from "remove" to "right-size" — the reason exploration findings are treated as leads.
#
# Verified direct imports 2026-08-21. Of the 8 google-* distributions installed, only
# google-genai is imported (`from google import genai`); google-generativeai,
# google-ai-generativelanguage, google-api-python-client and google-auth-httplib2 have
# no consumer here.
dependencies = [
  "google-genai",
  "openai",
  "anthropic",
  "pandas",
  "matplotlib",
  "python-dotenv",
  "tqdm",
]

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.uv]
# --- BEGIN generated constraint-dependencies (make -C ~/dev/deps sync-constraints) ---
# 51 pins: this project's resolved closure intersected with the shared set.
constraint-dependencies = [
  "annotated-types==0.8.0",
  "anthropic==1.0.0",
  "anyio==4.14.2",
  "certifi==2026.7.22",
  "cffi==2.1.1",
  "charset-normalizer==3.5.1",
  "contourpy==1.3.3",
  "cryptography==50.0.0",
  "cycler==0.12.1",
  "distro==1.9.0",
  "docstring-parser==0.18.0",
  "fonttools==4.63.0",
  "google-auth==2.56.3",
  "google-genai==2.19.0",
  "h11==0.16.0",
  "httpcore==1.0.9",
  "httpcore2==2.12.0",
  "httpx==0.28.1",
  "httpx2==2.12.0",
  "idna==3.19",
  "iniconfig==2.3.0",
  "jiter==0.16.0",
  "kiwisolver==1.5.0",
  "matplotlib==3.11.1",
  "numpy==2.5.2",
  "openai==2.54.0",
  "packaging==26.3",
  "pandas==3.0.5",
  "pillow==11.3.0",
  "pluggy==1.6.0",
  "pyasn1==0.6.4",
  "pyasn1-modules==0.4.2",
  "pycparser==3.0",
  "pydantic==2.13.4",
  "pydantic-core==2.46.4",
  "pygments==2.21.0",
  "pyparsing==3.3.2",
  "pytest==9.1.1",
  "python-dateutil==2.9.0.post0",
  "python-dotenv==1.2.3",
  "requests==2.34.2",
  "ruff==0.16.4",
  "six==1.17.0",
  "sniffio==1.3.1",
  "tenacity==9.1.4",
  "tqdm==4.70.0",
  "truststore==0.10.4",
  "typing-extensions==4.16.0",
  "typing-inspection==0.4.4",
  "urllib3==2.7.0",
  "websockets==16.1.1",
]
# --- END generated constraint-dependencies ---
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
